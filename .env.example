# ============================================
# SEGURIDAD - OBLIGATORIO
# ============================================

# Secret Key de Django
# Genera una nueva clave secreta con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=

# Modo Debug (False en producción, True en desarrollo)
DEBUG=True

# Hosts permitidos (separados por comas, sin espacios)
# En desarrollo: localhost,127.0.0.1
# En producción: tu-dominio.com,www.tu-dominio.com
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================
# CONFIGURACIÓN DE EMAIL (Opcional)
# ============================================

# Backend de email
# Desarrollo: django.core.mail.backends.console.EmailBackend (muestra en consola)
# Producción: django.core.mail.backends.smtp.EmailBackend (envía emails reales)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# Configuración SMTP (solo necesario si EMAIL_BACKEND es smtp)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu_email@gmail.com
# EMAIL_HOST_PASSWORD=tu_contraseña_de_aplicacion
# DEFAULT_FROM_EMAIL=noreply@bibliotecacolectiva.com

# ============================================
# NOTAS
# ============================================
# 
# 1. NUNCA commitees el archivo .env al repositorio
# 2. En producción, asegúrate de que DEBUG=False
# 3. Genera una SECRET_KEY única para cada entorno
# 4. Para Gmail, necesitas una "Contraseña de aplicación" en lugar de tu contraseña normal
# 5. En desarrollo, los emails se mostrarán en la consola del servidor
